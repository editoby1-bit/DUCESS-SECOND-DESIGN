import type { DucessDataGateway } from "../../gateway/contract";

export function createSupabaseAdapter(): DucessDataGateway {
  throw new Error("Supabase adapter is intentionally skeleton-only in Phase 3B.1");
}
